name: flutter_music_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # Основные рабочие пакеты
  shared_preferences: ^2.0.15
  provider: ^6.0.5
  http: ^0.13.5
  path_provider: ^2.0.15
  cached_network_image: ^3.2.3
  pull_to_refresh: ^2.0.0
  oktoast: ^3.2.4
  shimmer: ^2.0.0
  bubble_bottom_bar: ^2.0.0
  localstorage: ^4.0.1
  dio: ^4.0.6

  # Аудио с совместимой версией
  audioplayers: ^0.20.1

  # Информация об устройстве и пакете
  device_info: ^2.0.3
  package_info: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
